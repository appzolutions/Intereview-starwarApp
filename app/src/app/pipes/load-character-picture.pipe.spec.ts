import { LoadCharacterPicturePipe } from './load-character-picture.pipe';

describe('LoadCharacterPicturePipe', () => {
  it('create an instance', () => {
    const pipe = new LoadCharacterPicturePipe();
    expect(pipe).toBeTruthy();
  });
});
